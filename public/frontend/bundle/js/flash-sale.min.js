/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!*************************************************!*\
  !*** ./resources/src/frontend/js/flash-sale.js ***!
  \*************************************************/
$('[data-flash-sale-id]').each(function (_, element) {
  var flashSaleId = $(this).attr('data-flash-sale-id');
  var flashSaleEndAt = $(this).attr('data-flash-sale-end-at');
  var $day = $("[data-flash-sale-days-id=\"".concat(flashSaleId, "\"]"));
  var $hour = $("[data-flash-sale-hours-id=\"".concat(flashSaleId, "\"]"));
  var $minute = $("[data-flash-sale-minutes-id=\"".concat(flashSaleId, "\"]"));
  var $second = $("[data-flash-sale-seconds-id=\"".concat(flashSaleId, "\"]"));
  var isOngoingFlashSale = $(this).attr('data-isongoing-flash-sale') == 'true';
  if (!isOngoingFlashSale) {
    return;
  }
  var timer = null;
  timer = setInterval(function () {
    __countdown__(flashSaleEndAt, function (_ref) {
      var days = _ref.days,
        hours = _ref.hours,
        minutes = _ref.minutes,
        seconds = _ref.seconds;
      days = days.toString().length <= 1 ? "0".concat(days) : days;
      hours = hours.toString().length <= 1 ? "0".concat(hours) : hours;
      minutes = minutes.toString().length <= 1 ? "0".concat(minutes) : minutes;
      seconds = seconds.toString().length <= 1 ? "0".concat(seconds) : seconds;
      $day.html(days);
      $hour.html(hours);
      $minute.html(minutes);
      $second.html(seconds);
      if (+days == 0 && +hours == 0 && +minutes == 0 && +seconds == 0) {
        clearInterval(timer);
      }
    });
  }, 1000);
});
function __countdown__(endDate) {
  var callback = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function () {
    return undefined;
  };
  var __endDate = new Date(endDate).getTime();
  var __nowDate = new Date().getTime();
  var __distance = __endDate - __nowDate;
  var days = Math.floor(__distance / (1000 * 60 * 60 * 24));
  var hours = Math.floor(__distance % (1000 * 60 * 60 * 24) / (1000 * 60 * 60));
  var minutes = Math.floor(__distance % (1000 * 60 * 60) / (1000 * 60));
  var seconds = Math.floor(__distance % (1000 * 60) / 1000);
  days = days < 0 ? 0 : days;
  hours = hours < 0 ? 0 : hours;
  minutes = minutes < 0 ? 0 : minutes;
  seconds = seconds < 0 ? 0 : seconds;
  return callback({
    days: days,
    hours: hours,
    minutes: minutes,
    seconds: seconds
  });
}
/******/ })()
;