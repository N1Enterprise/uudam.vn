/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!**************************************************!*\
  !*** ./resources/src/frontend/js/menu-drawer.js ***!
  \**************************************************/
$(document).ready(function () {
  $.each($('nav.header__inline-menu .horizontal-mega-menu .app-menu-item'), function (index, element) {
    var _$submenu$offset, _$mainHeaderHeading$o;
    var $menu = $(element);
    var $submenu = $menu.find('.mm-submenu');
    var $mainHeaderHeading = $('#shopify-section-header .header__heading');
    var $appHeader = $('header.header');
    var offsetLeft = ((_$submenu$offset = $submenu.offset()) === null || _$submenu$offset === void 0 ? void 0 : _$submenu$offset.left) - ((_$mainHeaderHeading$o = $mainHeaderHeading.offset()) === null || _$mainHeaderHeading$o === void 0 ? void 0 : _$mainHeaderHeading$o.left);
    var withOfAppHeader = $appHeader.width();
    $submenu.css('left', "-".concat(offsetLeft, "px"));
    $submenu.css('width', "".concat(withOfAppHeader, "px"));
  });
  $.each($('sticky-header .menu-drawer__menu a.menu-drawer__menu-item.list-menu__item.link--text .toggle-menu-btn'), function (index, element) {
    var expanedMenuButton = $(element);
    var menuLink = $(element).parent('a');
    menuLink.on('click', function (e) {
      e.preventDefault();
      var tagTarget = e.target;
      if ($(tagTarget).hasClass('toggle-menu-btn') || $(tagTarget).parents('.toggle-menu-btn').hasClass('toggle-menu-btn')) {
        var menuItem = $(tagTarget).parents('li.app-menu-item');
        menuItem.toggleClass('mm-hovering');
        menuItem.find('.mm-arrow-icon').toggleClass('mm-cross');
        menuItem.find('ul.mm-submenu').toggleClass('submenu-opened');
        menuItem.find('ul.mm-submenu').css({
          'max-height': menuItem.find('ul.mm-submenu').hasClass('submenu-opened') ? '1485px' : '0'
        });
        menuItem.find('ul.mm-submenu .mega-menu-item-container').parent().toggleClass('mmBounceInUp');
        menuItem.find('[data-menu-type="post"]').find('.mm-image-container').css({
          height: '180px'
        });
        menuItem.find('[data-menu-type="post"]').find('.mm-image').css({
          height: '180.062px',
          width: '344px'
        });
      } else {
        var href = $(this).attr('href');
        if (href) {
          window.location.href = href;
        }
      }
    });
  });
});
$('#Details-menu-drawer-container .header__icon').on('click', function () {
  $(this).attr('aria-expanded', $('#Details-menu-drawer-container').attr('open') !== undefined ? 'false' : 'true');
});
/******/ })()
;